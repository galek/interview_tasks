const someObject = {
  b: 1,
  c: 2
};

// Что это такое-деструкторизация и что будет если передать объект {a:0,b:1,c:2} => будут доступны переменные a->0,b->1;c->2
const {a, ...b} = someObject;

// Какой вывод будет? Если передать объект someObject с полями выше
// a-undefined
// b:1
// c:2
